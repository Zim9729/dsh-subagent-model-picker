# dsh-subagent-model-picker bundle patch
# The DSH profile loader reads this file through dsh.bundle.patch.
- insert:
    - id: subagent-model-override
      name: '@zim9729/dsh-subagent-model-picker'
